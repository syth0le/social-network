logger:
  level: "debug"
  encoding: "console"
  path: "stdout"

server:
  enable: true
  endpoint: "localhost"

storage:
  host: "localhost"
  port: 5432
  database: "social-network-local"
  username: "social-network-local-admin"
  ssl_mode: "disable"
  connection_attempts: 3
